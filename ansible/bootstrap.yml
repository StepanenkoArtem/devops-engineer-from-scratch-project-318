# 1) Login as root using ssh
# 2) Create new user
# 3) setup access for the new user (use vars for user name and local public key)
# 4) Config permission for new user
# 4) enable in ufw new SSH port (use vars)
# 5) configure sshd to use new port (use vars)
# 5) logout from root

# 6) login as new user using old port
# 7) disable login with password for new user
# 8) disable login as root
# 9) restart sshd 

# 10) login by new user via new port
# 11) configure firewall to disable old ssh port
